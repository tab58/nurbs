'use strict';

// var curveDerivCpts = require('./alg3-3.js');

var getSurfDerivCtrlPtsGeneric = function getSurfDerivCtrlPtsGeneric (p, U, q, V, P, u, v, d, r1, r2, s1, s2, PKL, vec) {
  // var du = Math.min(d, p);
  // var dv = Math.min(d, q);
  // var r = r2 - r1;
  // var s = s2 - s1;
  // var j = 0;
  // var temp;
  // for (j = s1; j <= s2; ++j) {
  //   temp =
  //   curveDerivCpts(n, p, U, /*&P[][j]*/, du, r1, r2, temp);
  // }
  // for (k = 0; k < du; ++k) {

  // }
  throw new Error('Not implemented yet.');
};

module.exports = getSurfDerivCtrlPtsGeneric;
